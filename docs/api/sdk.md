# SDK — `arcana.run()`

The simplest way to use Arcana. One async call, one result.

For multi-turn sessions, pipelines, or batch execution, use [`Runtime`](runtime.md).

## `arcana.run`

::: arcana.sdk.run
    options:
      show_root_full_path: false

## `RunResult`

The dataclass returned by `arcana.run()` and `Runtime.run()`.

::: arcana.sdk.RunResult
    options:
      show_root_full_path: false
